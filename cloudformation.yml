#AWSTemplateFormatVersion: '2010-09-09'
#Description: EC2 server for DevOps demo with Apache and CodeDeploy agent installed.

#Parameters:
 # KeyName:
  #  Type: String
 #   Description: Name of an existing EC2 key pair

#Resources:

#  DevOpsInstanceRole:
#    Type: AWS::IAM::Role
#    Properties:
#      RoleName: DevOpsInstanceRole
#      AssumeRolePolicyDocument:
#        Version: '2012-10-17'
#        Statement:
#          - Effect: Allow
#            Principal:
#              Service: ec2.amazonaws.com
#            Action: sts:AssumeRole
#      ManagedPolicyArns:
#        - arn:aws:iam::aws:policy/service-role/AmazonEC2RoleforAWSCodeDeploy
#        - arn:aws:iam::aws:policy/AmazonS3FullAccess#
#
# DevOpsInstanceProfile:
#    Type: AWS::IAM::InstanceProfile
#   Properties:
#      Roles:
#        - !Ref DevOpsInstanceRole
#      InstanceProfileName: DevOpsInstanceProfile
#
# DevOpsSecurityGroup:
#    Type: AWS::EC2::SecurityGroup
#    Properties:
#      GroupDescription: Enable HTTP access
#      SecurityGroupIngress:
#        - IpProtocol: tcp
#          FromPort: 80
#          ToPort: 80
#          CidrIp: 0.0.0.0/0
#
#  DevOpsEC2Instance:
#    Type: AWS::EC2::Instance
#    Properties:
#      InstanceType: t2.micro
#      ImageId: ami-083d8a6500c1d55d6
#      KeyName: !Ref KeyName
#      IamInstanceProfile: !Ref DevOpsInstanceProfile
#      SecurityGroupIds:
#        - !Ref DevOpsSecurityGroup
#      Tags:
#        - Key: Name
#          Value: DevOpsDemoServer
#      UserData:
#        Fn::Base64: !Sub |
#          #!/bin/bash
#          yum update -y
#          yum install -y httpd
#          systemctl start httpd
#          systemctl enable httpd
#          yum install -y ruby wget
#          cd /home/ec2-user
#          wget https://aws-codedeploy-${AWS::Region}.s3.${AWS::Region}.amazonaws.com/latest/install
#          chmod +x ./install
#          ./install auto
#          systemctl enable codedeploy-agent
#          systemctl start codedeploy-agent
#          echo "httpd and codedeploy installed!"
